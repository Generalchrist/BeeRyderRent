import { ApiImagesPipe } from './api-images.pipe';

describe('ApiImagesPipe', () => {
  it('create an instance', () => {
    const pipe = new ApiImagesPipe();
    expect(pipe).toBeTruthy();
  });
});
